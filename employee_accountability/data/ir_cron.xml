<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ============================================= -->
    <!-- Scheduled Action: Cleanup Expired Sessions -->
    <!-- ============================================= -->
    <record id="ir_cron_cleanup_employee_sessions" model="ir.cron">
        <field name="name">Employee Accountability: Cleanup Expired Sessions</field>
        <field name="model_id" ref="model_employee_session_context"/>
        <field name="state">code</field>
        <field name="code">model._cron_cleanup_expired_sessions()</field>
        <field name="interval_number">6</field>
        <field name="interval_type">hours</field>
        <field name="active">True</field>
    </record>
</odoo>
